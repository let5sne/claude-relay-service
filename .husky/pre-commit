#!/usr/bin/env sh

echo "Running lint-staged (Prettier) for staged files..."
# Run only if lint-staged is available locally; otherwise skip to avoid failing hooks
if node -e "require.resolve('lint-staged')" >/dev/null 2>&1; then
  npx --no-install lint-staged
else
  echo "lint-staged not installed locally; skipping."
fi
